# CC会话管理中心 — 一键部署

## 是什么

扫描所有 Claude Code 会话 JSONL，生成单一 HTML 页面。左侧会话列表+右侧聊天内容，支持搜索、分类、置顶、归档、收藏、文件夹、双主题。

## 一键安装（推荐）

在新电脑的 Claude Code 中粘贴下面这段话：

```
请帮我安装 CC会话管理中心。步骤：

1. 从 GitHub 下载三个脚本到 ~/.claude/shared-scripts/session/ 目录：
   - https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/export-all-html.py
   - https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/track-iteration.py
   - https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/upgrade-html-v3.py
   用 curl 下载，mkdir -p 先建目录。

2. 在 ~/.claude/settings.json 中配置两个 Hook：
   - UserPromptSubmit: python ~/.claude/shared-scripts/session/export-all-html.py --quick
   - SessionEnd: python ~/.claude/shared-scripts/session/export-all-html.py
   如果已有其他 Hook，合并进去不要覆盖。

3. 立即运行一次全量导出：
   python ~/.claude/shared-scripts/session/export-all-html.py
```

CC 会自动完成全部安装，安装完成后桌面会出现 `CC会话管理中心.html`。

## 手动安装

### 1. 下载脚本

```bash
mkdir -p ~/.claude/shared-scripts/session/
curl -o ~/.claude/shared-scripts/session/export-all-html.py https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/export-all-html.py
curl -o ~/.claude/shared-scripts/session/track-iteration.py https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/track-iteration.py
curl -o ~/.claude/shared-scripts/session/upgrade-html-v3.py https://raw.githubusercontent.com/1992686510qq-hash/cc-session-center/main/upgrade-html-v3.py
```

### 2. 配置 Hook

在 `~/.claude/settings.json` 中添加：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {"command": "python ~/.claude/shared-scripts/session/export-all-html.py --quick"}
    ],
    "SessionEnd": [
      {"command": "python ~/.claude/shared-scripts/session/export-all-html.py"}
    ]
  }
}
```

### 3. 首次运行

```bash
python ~/.claude/shared-scripts/session/export-all-html.py
```

## Hook 机制

| Hook | 触发时机 | 耗时 | 作用 |
|------|----------|------|------|
| UserPromptSubmit | 每次发消息 | <1秒 | 增量更新当前会话 |
| SessionEnd | 会话结束 | ~5秒 | 全量扫描兜底 |

## 工作原理

```
~/.claude/projects/ 下所有 .jsonl
  → export-all-html.py 扫描提取
  → 消息解析 + 自动分类 + Agent树 + 引用/产出物
  → 嵌入 HTML 模板（单文件，双击即用）
  → 输出到桌面
```

## 文件说明

| 文件 | 行数 | 用途 |
|------|------|------|
| `export-all-html.py` | 2533 | 主脚本：扫描JSONL → 生成HTML |
| `track-iteration.py` | 222 | 迭代链追踪：跨会话关联 |
| `upgrade-html-v3.py` | 486 | HTML升级：旧版HTML升级到v3.0 |

## 前置条件

- Python 3.7+（零外部依赖，纯标准库）
- Claude Code 已使用过（存在 `~/.claude/projects/` 目录）
