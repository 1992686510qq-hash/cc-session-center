# CC会话管理中心 — 部署说明

## 是什么

一键扫描所有 Claude Code 会话 JSONL，生成单一 HTML 页面。左侧会话列表+右侧聊天内容，支持搜索、分类、置顶、归档、收藏、文件夹、双主题。

## 文件说明

| 文件 | 行数 | 用途 |
|------|------|------|
| `export-all-html.py` | 2533 | 主脚本：扫描JSONL → 生成HTML |
| `track-iteration.py` | 222 | 迭代链追踪：跨会话关联 |
| `upgrade-html-v3.py` | 486 | HTML升级：旧版HTML升级到v3.0三维组织 |

## 部署步骤

### 1. 放到任意目录

把这三个 `.py` 文件放到新电脑的 `~/.claude/shared-scripts/session/` 下：

```bash
mkdir -p ~/.claude/shared-scripts/session/
cp *.py ~/.claude/shared-scripts/session/
```

### 2. 配置 Hook（自动更新）

在 `~/.claude/settings.json` 中添加：

```json
{
  "hooks": {
    "UserPromptSubmit": [
      {
        "command": "python ~/.claude/shared-scripts/session/export-all-html.py --quick"
      }
    ],
    "SessionEnd": [
      {
        "command": "python ~/.claude/shared-scripts/session/export-all-html.py"
      }
    ]
  }
}
```

- `UserPromptSubmit`：每次发消息时增量更新（<1秒）
- `SessionEnd`：会话结束时全量扫描兜底（~5秒）

### 3. 首次运行

```bash
python ~/.claude/shared-scripts/session/export-all-html.py
```

输出默认到桌面：`~/Desktop/Claude Code/CC会话管理中心.html`

也可以指定输出目录：
```bash
python ~/.claude/shared-scripts/session/export-all-html.py ~/Documents/
```

## 工作原理

```
JSONL 会话文件
  → export-all-html.py 扫描 ~/.claude/projects/ 下所有 .jsonl
  → 提取消息、分类、引用、产出物、Agent树
  → 嵌入到 HTML 模板中（单文件，双击即用）
  → 输出 ～24MB HTML 文件
```

## 前置条件

- Python 3.7+（零外部依赖，只用标准库）
- Claude Code 会话数据在 `~/.claude/projects/` 下有 `.jsonl` 文件

## 可选：迭代链追踪

```bash
# 在父会话中保存桥接
python track-iteration.py save <session-id> "桥接文本"

# 新会话启动时匹配桥接
python track-iteration.py resolve <session-id>

# 查看待接收的桥接
python track-iteration.py pending
```
